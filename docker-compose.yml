version: '3.8'
services:
  actual-rest:
    build: .
    container_name: actual-rest
    ports:
      - "7006:7006"
    environment:
      - PORT=7006
      - ACTUAL_SERVER_URL=${ACTUAL_SERVER_URL}
      - ACTUAL_PASSWORD=${ACTUAL_PASSWORD}
    restart: unless-stopped
